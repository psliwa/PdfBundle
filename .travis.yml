language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7.0
  - hhvm

before_script:
  - composer self-update
  - composer --dev install

script: vendor/bin/phpunit --configuration Tests/phpunit.xml

matrix:
  allow_failures:
    - php: 7.0
    - php: hhvm